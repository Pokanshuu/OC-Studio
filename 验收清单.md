# 验收清单 — stability-update 分支

> 分支：`stability-update`（commit `fa648c4`）
> Dev 环境：**http://localhost:3000**（`npm run dev`）
> 验证前建议清空一次本地数据或用无痕窗口，避免旧数据干扰。

按下列逐项核对。标记 `[x]` 表示通过，`[ ]` 表示未通过（备注现象）。

---

## A. 数据安全（P0）

- [ ] **A1 内链弹层 XSS**
  1. 世界观新建词条，标题输入 `<img src=x onerror=alert(1)>`
  2. 回到角色编辑器，输入 `[[`
  3. 预期：弹层里该标题**按纯文本**显示，不弹窗、不执行脚本

- [ ] **A2 Tauri 下 HEIF 转 JPEG**（需桌面 Tauri 环境）
  1. 上传一张 `.heic` 图片
  2. 预期：落盘文件为 JPEG（非原始 HEIF），图片可正常显示

- [ ] **A3 CountryEditor 无限 refetch**
  1. 进入「国家」→ 打开任一国家编辑器
  2. 打开 DevTools Network 面板观察几秒
  3. 预期：无持续刷新的 `countries` 请求风暴，只在进入时加载一次

- [ ] **A4 导入失败回滚**
  1. 准备一个含**重复 id** 的坏 `.ocbak`（可复制正常备份后手工把两条记录改成同一 id）
  2. 导入选择「清空并导入」
  3. 预期：报错后**原数据仍在**，未被清空

- [ ] **A5 导出/导入含时期（periods）**
  1. 时间线里创建一条「时期」
  2. 导出 `.ocbak`，用文本编辑器打开 JSON，确认含 `"periods": [...]`
  3. 清空导入该备份，预期时期恢复

- [ ] **A6 Capacitor 导出不假成功**（需 Android）
  1. 导出 `.ocbak`
  2. 预期：`导出成功` toast 在文件真正写入后出现；写盘失败会提示失败

## B. 交互缺陷（P1）

- [ ] **B1 Android 返回键层级**（需 Android 真机/模拟器）
  1. 依次进入「时间线 / 关系图 / 相册」，按系统返回键
  2. 预期：回到「角色」（gallery 首页），**不直接退出 App**；在首页再按才退出

- [ ] **B2 `[[` 内链懒注册**
  1. 全新会话，**不进入「世界观」板块**，直接新建角色
  2. 编辑器输入 `[[`
  3. 预期：能列出词条建议（旧 bug 会为空）

- [ ] **B3 关系图双向边去重**
  1. 打开「关系图」，找一个角色↔国家、事件↔国家都有关联的场景
  2. 预期：同一对节点之间**只有一条连线**，不再出现「属于」+「所属」两条反向边

- [ ] **B4 桌面右键不吞**
  1. 桌面浏览器，在非输入区（列表项/正文）右键
  2. 预期：出现**浏览器原生**右键菜单
  3. 在输入框/文本框右键，预期出现自定义「剪切/复制/粘贴/全选」菜单

- [ ] **B5 标签删除级联**
  1. 给多个角色/事件打同一标签，删除该标签
  2. 预期：删除后这些实体的标签列表不再显示该标签（无悬空 id）

- [ ] **B6 导入确认框不自动关闭**
  1. 选择一个导入文件，点「确认导入」
  2. 预期：对话框**保持打开**并显示「导入中...」，完成后才关闭并 toast 结果

- [ ] **B7 移动端输入长按菜单**（需移动端）
  1. 打开设置页，长按 API Key 输入框
  2. 预期：弹出「剪切/复制/粘贴/全选」菜单（旧 bug 因 Provider 断裂无反应）

## C. 重构 / 复用（无功能回归）

- [ ] **C1 data-updated 常量收敛**
  1. 正常增删改角色/事件/国家/词条/时期、恢复回收站、导入导出
  2. 预期：各列表**照常自动刷新**，无报错（回归验证）

- [ ] **C2 autoDarkMode 移除**
  1. 打开设置，切换主题（白天/夜间/跟随系统）
  2. 预期：主题切换正常，无异常

---

## 环境说明
- **A2 / A6 / B1 / B7** 需要 Tauri 桌面或 Android 环境。
- 其余项在浏览器 dev 环境即可验证。
- 全绿后可将 `stability-update` 合并回 `main`。
